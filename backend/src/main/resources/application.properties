spring.application.name=project_sphere
server.port=8081

#Database Connection Settings

spring.datasource.url=jdbc:mysql://localhost:3306/projectsphere?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Create or Update DB Tables automatically
spring.jpa.hibernate.ddl-auto=create
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Force JPA to use exact camelCase matching without underscores
#spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl

#Springboot Default HikariCP Configuration

spring.sql.init.mode=always
spring.jpa.defer-datasource-initialization=true

#?????????????????????????????